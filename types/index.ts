import { ButtonHTMLAttributes, ReactNode } from "react";

export type variantType = {
  fontSize: string;
  lineHeight: string;
};

export type User = {
  email: string;
  username: string;
  id: string;
  token: string;
};
